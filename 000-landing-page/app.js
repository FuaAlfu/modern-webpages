const contactButton = document.querySelector('.contact-button');
        contactButton.addEventListener('click', () => {
            alert('You clicked the Contact Us button!');
            // add more code to handle the contact form or other actions.
});